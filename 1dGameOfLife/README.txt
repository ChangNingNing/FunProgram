There is a 0/1 string, and play a role on it.
If a bit is 1, it would become 0.
If a bit is 0 and the neibor of it has exactly one 1, then it would become 1.  Otherwise it remains 0.

Then if the string becomes all 0's, terminated.
If the string gets into an infinite loop, then find the loop.
